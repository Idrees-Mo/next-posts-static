import styles from "../styles/Footer.module.scss";
const Footer = () => {
  return (
    <div className={styles.footer}>
      Build with <span>❤</span>& Next JS
    </div>
  );
};

export default Footer;
